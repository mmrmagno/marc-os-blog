name: marc-os-blog
services:
  app:
    image: ${IMAGE_TAG:-ghcr.io/mmrmagno/marc-os-blog:latest}
    container_name: marc-os-app
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    tmpfs:
      - /var/cache/nginx:size=20m,mode=0770,uid=101,gid=101
      - /var/run:size=2m,mode=0770,uid=101,gid=101
      - /tmp:size=10m,mode=1777
    networks:
      - proxy
    expose:
      - "8080"

    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

networks:
  proxy:
    name: ${PROXY_NETWORK:?PROXY_NETWORK required in .env}
    external: true
